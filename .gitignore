# Секрети та локальні налаштування — ніколи не комітити
.claude/settings.local.json
.env
.dev.vars

# Wrangler / Node
.wrangler/
node_modules/

# Локальні бекапи БД — містять персональні дані заявників, у git не місце
backups/

# Чернетки грантових заявок — робочі/особисті, не для публічного репозиторію
grants/
